# Copia este archivo a .env y pon la IP de red local que imprime el backend
# al arrancar (npm run start:dev en backend/), no localhost: el celular está
# en la red WiFi, no dentro de la misma máquina que el backend.
EXPO_PUBLIC_API_URL=http://192.168.1.100:3000
