// El renderer usa el mismo build web (React + axios) sin necesitar APIs de Node,
// así que este preload no expone nada por ahora; contextIsolation queda activo
// por seguridad y este archivo sirve de punto de extensión si se necesita en el futuro.
export {};
